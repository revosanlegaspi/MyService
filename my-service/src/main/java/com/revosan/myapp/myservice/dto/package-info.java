/**
 * ProductRequest DTO
 * 
 * A Data Transfer Object (DTO) is crucial for separating the API's input
 * contract from the internal domain model. It's used to define the data
 * structure expected from clients and is the ideal place to apply JSR 380 (Bean
 * Validation) annotations.
 */
package com.revosan.myapp.myservice.dto;